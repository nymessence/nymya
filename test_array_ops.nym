import crystal

// Simple array test
var test_list = []
test_list.append(42)
var first_elem = test_list[0]
crystal.manifest("First element: " + first_elem.to_string())

var list_size = test_list.length
crystal.manifest("List size: " + list_size.to_string())

