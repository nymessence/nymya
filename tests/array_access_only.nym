import crystal
import math

// Basic functionality to verify compiler stability
crystal.manifest("Testing NymyaLang compiler functionality...")

// Create a new test to ensure arrays work properly
var my_list = []
my_list.append(42)
my_list.append(24)

// Test with array method that has parentheses
// var list_size = my_list.length() // This might not work if .length() syntax is expected

// Instead, let's use the explicit length method with parentheses if needed
// For now, just test append functionality which should work
crystal.manifest("Array append worked successfully")

// Use a simple loop to access values the safe way
var index = 0
while (index < 2) {
    if (index == 0) {
        var first_val = my_list[0]
        crystal.manifest("First value: " + first_val.to_string())
    }
    if (index == 1) {
        var second_val = my_list[1]
        crystal.manifest("Second value: " + second_val.to_string())
    }
    index = index + 1
}