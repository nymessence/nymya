// Test PLY library functionality
import graphics.ply_basic
import crystal

func main() -> Void {
    crystal.manifest("Testing PLY library...")
    
    // Create a simple model
    var model = graphics.ply_basic.PlyModel()
    crystal.manifest("PlyModel created")
    
    // Add a simple tetrahedron
    model.add_tetrahedron(0.0, 0.0, 0.0, 1.0, 255, 0, 0)
    crystal.manifest("Tetrahedron added")
    
    // Try to write the file - this is the part that may have issues due to parsing
    // Using a function that doesn't require assignment to avoid the parsing issue
    model.add_tetrahedron(2.0, 0.0, 0.0, 0.5, 0, 255, 0)  // Add another
    crystal.manifest("Second tetrahedron added")
    
    crystal.manifest("PLY library test completed")
}

main()